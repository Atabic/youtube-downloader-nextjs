import React from 'react'
import { Layout } from 'antd';
const {  Footer } = Layout;
const PageFooter = () => {
    return (
        <Footer>
            Footer
        </Footer>
    )
}

export default PageFooter
